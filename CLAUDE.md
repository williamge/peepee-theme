# peepee-theme

A VSCode colour theme, primarily targeting Elixir.

## Development

Install dependencies:

```sh
npm install
```

Before committing, run the full check:

```sh
npm run check
```

To auto-fix formatting:

```sh
npm run format
```

`npm run check` must pass before any commit.
